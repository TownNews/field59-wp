; This file provides targeting information for deploying packages to the hosted environment

[global]
; This directive stores the code from from this package in an off-site repo in case
; the company is sold or shutdown. Per agreements with some customers, the code will
; be delivered to them from this escrow company.
escrow=1

;[example]
;cmsadminserver=noarch
;cmsappserver=noarch
;cmsmgrserver=noarch

;[example-devel]
;cvsserverphp5
;devbuildserver

;docserver
;eventserver
;gitserver

;[example-static]
;imgprocserver

;[example-log]
;logserver

;[example-cache]
;memcacheserver=noarch

;[example-search]
;searchrdserver
;searchwrserver
;solrserver
;solrzooserver
